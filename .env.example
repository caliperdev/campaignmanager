# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Read-only user (optional): single email that gets monitor-only view
READ_ONLY_MONITOR_EMAIL=view@buhomedia.com

# Full-access override (optional): comma-separated emails that always see full sidebar (campaigns, sources, home)
# Required on Vercel so emma sees same left pane as test
FULL_ACCESS_EMAILS=emma@buhomedia.com,test@buhomedia.com

# Dataverse integration (optional — button hidden when not set)
# DATAVERSE_TENANT_ID=
# DATAVERSE_CLIENT_ID=
# DATAVERSE_CLIENT_SECRET=
# DATAVERSE_ENVIRONMENT_URL=
