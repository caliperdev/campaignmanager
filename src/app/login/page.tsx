import LoginContent from "./LoginContent";

export const metadata = {
  title: "Login",
  description: "Sign in to Campaign Manager",
};

export default function LoginPage() {
  return <LoginContent />;
}
